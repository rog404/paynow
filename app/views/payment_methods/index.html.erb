<h1><%= t '.title' %></h1>
<% if @payment_methods.empty? %>
    <div class="alert alert-dark" role="alert">
        <%= t '.empty' %>
    </div>
<% end %>
<ul class="list-group">
    <% @payment_methods.each do |payment_method| %>
        <li class="list-group-item d-flex justify-content-between align-items-start">
                <div class="row row-cols-lg-5 gx-3 container justify-content-center">
                    <div class="col-3 text-center align-self-center">
                        <div class="fw-bold"><%= payment_method.description %></div>
                        <div class="card-text"><%= t payment_method.type %></div>
                    </div>
                    <div class="col-3 text-center align-self-center">
                        <div class="fw-bold"><%= t '.fee' %></div>
                        <div class="card-text"><%= number_to_percentage payment_method.fee, :precision => 2 %></div>
                    </div>
                    <div class="col-3 text-center align-self-center">
                        <div class="fw-bold"><%= t '.max_money_fee' %></div>
                        <div class="card-text"><%= number_to_currency payment_method.max_money_fee %></div>
                    </div>
                    <div class="col-3 text-center align-self-center">
                        <span class="badge  <%= payment_method.active? ? 'bg-success' : 'bg-dark' %> rounded-pill"><%= PaymentMethod.human_attribute_name "state.#{payment_method.state}" %></span>
                    </div>
                    <div class="col-3 text-center align-self-center">
                        <%= link_to t('.edit'), companies_path, class: 'btn btn-dark my-3' %>
                    </div>
                </div>
        </li>
    <% end %>
    <%= link_to 'Adicionar', new_company_path, class: 'btn btn-dark my-3' %>
</ul>
